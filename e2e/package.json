{
	"name": "kurrier-e2e",
	"private": true,
	"description": "End-to-end regression suite. Runs against the dev stack (db/docker-compose.dev.yml + pnpm dev) plus the e2e companions (docker-compose.e2e.yml). Standalone on purpose: not part of the pnpm workspace, zero impact on app lockfiles.",
	"scripts": {
		"test": "playwright test",
		"test:ui": "playwright test --ui"
	},
	"devDependencies": {
		"@playwright/test": "^1.55.0"
	}
}
